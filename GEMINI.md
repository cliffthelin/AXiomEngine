### SOURCE:
# Reversa

> Reverse engineering framework installed in this project.

## How to use

Type `/reversa` to activate Reversa and start or resume the project analysis.

## Behavior on activation

When the user types `/reversa` or the word `reversa` alone in a message:

1. Activate the `reversa` skill available in `.agents/skills/reversa/SKILL.md`
2. Read the SKILL.md in its entirety and follow the Reversa instructions exactly.

## Non-negotiable rule

Never delete, modify, or overwrite pre-existing files in the legacy project.
Reversa only writes to `.reversa/` and `_reversa_sdd/`.


---

### SOURCE:
# Reversa

> Reverse engineering framework installed in this project.

## How to use

Type `/reversa` to activate Reversa and start or resume the project analysis.

## Behavior on activation

When the user types `/reversa` or the word `reversa` alone in a message:

1. Activate the `reversa` skill available in `.agents/skills/reversa/SKILL.md`
2. Read the SKILL.md in its entirety and follow the Reversa instructions exactly.

## Non-negotiable rule

Never delete, modify, or overwrite pre-existing files in the legacy project.
Reversa only writes to `.reversa/` and `_reversa_sdd/`.
