```markdown
# Template — _reversa_sdd/questions.md

This file is generated by the Revisor and filled in by the user.

---

## How the Revisor creates this file

For each gap 🔴 that only the user can resolve, create an entry:

```markdown
## Question [N]

**Context:** [Where in the system did this question arise — module, function, line]
**Affected Spec:** [`_reversa_sdd/<unit>/{requirements|design|tasks}.md`]
**Question:** [Direct, unambiguous question]
**Impact:** [What changes in the spec depending on the answer]

**Answer:** <!-- fill in here -->
```

---

## How the user responds

The user should:
1. Fill in the `**Answer:**` field for each question.
2. Notify the Revisor when finished (type `reversa` or "answered the questions").

---

## How the Revisor processes the answers

After receiving notification from the user:
1. Read the complete file.
2. For each question with a filled-in answer:
   - Update the corresponding spec.
   - Reclassify the item according to the answer (🔴→🟢 or 🔴→🟡).
   - Mark the question as `✅ Answered` above the Answer field.
3. For unanswered questions: keep them as 🔴 in the final report.
4. Update `confidence-report.md` with the new percentages.

---

## Example of a generated file

```markdown
# Questions for Validation — [Project Name]

> Generated by the Revisor on [date]
> Answer each question and let me know when you are finished.

---

## Question 1

**Context:** `auth` module — `validateSession()` function in `src/auth/session.ts:47`
**Affected Spec:** [`_reversa_sdd/auth/requirements.md`]
**Question:** Does the session expire due to inactivity or only after a fixed absolute time? The code uses `lastActivity` but there is no visible expiration logic.
**Impact:** If it expires due to inactivity, the spec needs to include the maximum idle time and the behavior when renewing the session.

**Answer:** <!-- fill in here -->

---

## Question 2

**Context:** `orders` module — constant `MAX_ITEMS_PER_ORDER = 50` in `src/orders/constants.ts:12`
**Affected Spec:** [`_reversa_sdd/orders/requirements.md`]
**Question:** Is the limit of 50 items per order a business rule or a temporary technical limit? Are there plans to increase it?
**Impact:** If it is a business rule, it needs to be specified as 🟢 in the constraints. If it is technical, it should be 🟡 with a note for future review.

**Answer:** <!-- fill in here -->
```