# Governance Audit Report: 3ee1e423

- **Goal**: Run reversa-writer on /mnt/UBUNTU_8TB/Projects/SPLinkForge for project SPLinkForge
- **Scope**: `/mnt/UBUNTU_8TB/Projects/SPLinkForge`
- **Skill**: `reversa-writer`
- **Model**: `qwen3.6:27b` on P40

## LLM Rationale

👋 **Reversa Writer Activated**

To execute **Step 1: Assemble the plan** accurately, I must read the Reversa configuration and context files as defined in my operational contract. These files are not present in the current context.

Please provide the contents of the following files from your `/mnt/UBUNTU_8TB/Projects/SPLinkForge/.reversa/` directory:
1. `state.json` (for `output_folder`, `doc_level`, `doc_language`)
2. `config.toml` (for `[specs].granularity`)
3. `config.user.toml` (if it overrides granularity/folders)
4. `context/surface.json` (for `modules`, `features`, and `organization_suggestion`)

Additionally, please share any files already generated in the `<output_folder>` (default: `_reversa_sdd/`) and `.reversa/context/` by previous agents (Scout/Archaeologist).

**💡 Preliminary Analysis from Source Context:**
Based on the provided file tree and samples, SPLinkForge appears to be a complex Python data linkage application utilizing:
- **GUI Layer:** Custom Tkinter (`gui.core.main_window`)
- **Data Engine:** DuckDB (`staging.duckdb`, `Build_Registry`)
- **Linkage Core:** Splink engine integrations & stage runners
- **Environment:** Strict venv enforcement (`run_app.py`), cross-platform compatibility scripts, DuckDB registry tracking

Once you paste the configuration files, I will immediately:
1. Resolve the unit list according to the configured `granularity`
2. Calculate the exact file breakdown (3 canonical + applicable optionals)
3. Present the **Generation Plan** for your approval before generating a single file.

Please paste the configs when ready.