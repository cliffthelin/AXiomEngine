# Active ARCHON-TEST-RELEASE Checklist: SKILL.md

- [ ] G-ARCHON-TEST-RELEASE-SKILL-001: `brew` — Homebrew tap on macOS (tests the formula and checksums) (Section: Test Release)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-002: `curlmac` — `curl install.sh` on macOS (tests the install script, sandboxed to a temp dir) (Section: Test Release)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-003: `curlvps` — `curl install.sh` on a remote Linux VPS (tests the Linux binary and full install path) (Section: Test Release)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-004: When NOT to use this skill: (Section: Test Release)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-005: There is no release yet — build a local binary via `bash scripts/buildbinaries.sh` and run it from `dist/binaries/` directly (Section: Test Release)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-006: You want to test the dev clone — use `bun run validate` or invoke source directly via `bun packages/cli/src/cli.ts` (Section: Test Release)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-007: You want to test the full server + web UI deploy flow — use the cloudinit from `deploy/cloudinit.yml` on a real VPS (Section: Test Release)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-008: brew: `brew version` must succeed. If not, abort with "Homebrew not installed — see https://brew.sh/" (Section: Phase 2 — Pre-flight)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-009: curlmac: `curl version` must succeed (effectively always true on macOS) (Section: Phase 2 — Pre-flight)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-010: curlvps: `ssh <target> 'uname a'` must succeed. If not, abort with "Cannot SSH to <target>". Also verify `ssh <target> 'command v curl'` returns a path. (Section: Phase 2 — Pre-flight)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-011: Pass criteria: (Section: Test 1 — Version reports correctly)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-012: Exit code 0 (Section: Test 1 — Version reports correctly)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-013: Output contains `Archon CLI v<expectedversion>` (Section: Test 1 — Version reports correctly)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-014: Output contains `Build: binary` (not `Build: source (bun)`) (Section: Test 1 — Version reports correctly)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-015: Output contains a non`unknown` git commit (i.e., `Git commit: <sha>`) (Section: Test 1 — Version reports correctly)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-016: Common failures: (Section: Test 1 — Version reports correctly)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-017: Exit code nonzero → pinopretty crash (#960) or similar startup failure (Section: Test 1 — Version reports correctly)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-018: Wrong version reported → binary is stale or the build script failed to update `bundledbuild.ts` (Section: Test 1 — Version reports correctly)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-019: `Build: source (bun)` → `BUNDLED_IS_BINARY` was not set to `true` during the build (regression of #979) (Section: Test 1 — Version reports correctly)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-020: `Git commit: unknown` → build script did not capture the commit (Section: Test 1 — Version reports correctly)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-021: Pass criteria: (Section: Test 2 — Bundled workflows load)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-022: Exit code 0 (Section: Test 2 — Bundled workflows load)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-023: Output lists at least 20 bundled workflows (archonassist, archonfixgithubissue, archoncomprehensiveprreview, etc.) (Section: Test 2 — Bundled workflows load)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-024: No errors about missing workflow files or JSON parse failures (Section: Test 2 — Bundled workflows load)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-025: Common failures: (Section: Test 2 — Bundled workflows load)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-026: Empty list → bundled defaults were not embedded in the binary (regression of the `isBinaryBuild` detection path) (Section: Test 2 — Bundled workflows load)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-027: `Not in a git repository` → working directory handling bug (Section: Test 2 — Bundled workflows load)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-028: Parse errors → the embedded JSON is corrupt or stale (Section: Test 2 — Bundled workflows load)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-029: Prerequisite. Compiled binaries require Claude Code installed on the host and a configured binary path. Before running this test, ensure one of: (Section: Test 3 — SDK path works (assist workflow))
- [ ] G-ARCHON-TEST-RELEASE-SKILL-030: Pass criteria: (Section: claudeBinaryPath: /absolute/path/to/claude)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-031: Exit code 0 (Section: claudeBinaryPath: /absolute/path/to/claude)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-032: The Claude subprocess spawns successfully (no `spawn EACCES`, `ENOENT`, or `process exited with code 1` in the early output) (Section: claudeBinaryPath: /absolute/path/to/claude)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-033: No `Claude Code CLI not found` error (that means the resolver rejected the configured path — verify the cli.js actually exists) (Section: claudeBinaryPath: /absolute/path/to/claude)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-034: A response is produced (any response — even just "hello" — proves the SDK roundtrip works) (Section: claudeBinaryPath: /absolute/path/to/claude)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-035: Common failures: (Section: claudeBinaryPath: /absolute/path/to/claude)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-036: `Claude Code not found` → `CLAUDE_BIN_PATH` / `claudeBinaryPath` is unset or points at a nonexistent file. Fix the path and rerun. (Section: claudeBinaryPath: /absolute/path/to/claude)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-037: `Module not found "/Users/runner/..."` → regression of #1210: the resolver was bypassed and the SDK's `import.meta.url` fallback leaked a buildhost path. Investigate `packages/providers/src/claude/provider.ts` and the resolver. (Section: claudeBinaryPath: /absolute/path/to/claude)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-038: `Credit balance is too low` → auth is pointing at an exhausted API key (check `CLAUDE_USE_GLOBAL_AUTH` and `~/.archon/.env`) (Section: claudeBinaryPath: /absolute/path/to/claude)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-039: `unable to determine transport target for "pinopretty"` → #960 regression, binary crashes on TTY (Section: claudeBinaryPath: /absolute/path/to/claude)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-040: `package.json not found (bad installation?)` → #961 regression, `isBinaryBuild` detection broken (Section: claudeBinaryPath: /absolute/path/to/claude)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-041: Process exits before producing output → generic spawn failure, capture stderr (Section: claudeBinaryPath: /absolute/path/to/claude)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-042: Pass criteria (when no `~/.archon/config.yaml` configures `claudeBinaryPath`): (Section: Test 3b — Resolver error path (run without `CLAUDE_BIN_PATH`))
- [ ] G-ARCHON-TEST-RELEASE-SKILL-043: Error message contains `Claude Code not found` (Section: Test 3b — Resolver error path (run without `CLAUDE_BIN_PATH`))
- [ ] G-ARCHON-TEST-RELEASE-SKILL-044: Error message mentions both `CLAUDE_BIN_PATH` and `claudeBinaryPath` as remediation options (Section: Test 3b — Resolver error path (run without `CLAUDE_BIN_PATH`))
- [ ] G-ARCHON-TEST-RELEASE-SKILL-045: No `Module not found` stack traces referencing the CI filesystem (Section: Test 3b — Resolver error path (run without `CLAUDE_BIN_PATH`))
- [ ] G-ARCHON-TEST-RELEASE-SKILL-046: Pass criteria: (Section: Test 4 — Env-leak gate refuses a leaky .env (optional, for releases including 1036/1038/983))
- [ ] G-ARCHON-TEST-RELEASE-SKILL-047: The command exits with a nonzero code, OR produces an error message containing `Cannot add codebase` or `Cannot run workflow` (Section: Test 4 — Env-leak gate refuses a leaky .env (optional, for releases including 1036/1038/983))
- [ ] G-ARCHON-TEST-RELEASE-SKILL-048: The error mentions the dangerous key name (`ANTHROPIC_API_KEY`) (Section: Test 4 — Env-leak gate refuses a leaky .env (optional, for releases including 1036/1038/983))
- [ ] G-ARCHON-TEST-RELEASE-SKILL-049: No Claude subprocess was actually spawned (the gate shortcircuited) (Section: Test 4 — Env-leak gate refuses a leaky .env (optional, for releases including 1036/1038/983))
- [ ] G-ARCHON-TEST-RELEASE-SKILL-050: Common failures: (Section: Test 4 — Env-leak gate refuses a leaky .env (optional, for releases including 1036/1038/983))
- [ ] G-ARCHON-TEST-RELEASE-SKILL-051: Command proceeds normally → the envleak gate is not active (regression of #1036) (Section: Test 4 — Env-leak gate refuses a leaky .env (optional, for releases including 1036/1038/983))
- [ ] G-ARCHON-TEST-RELEASE-SKILL-052: Error is generic or unclear → the contextaware error message from #983 has regressed (Section: Test 4 — Env-leak gate refuses a leaky .env (optional, for releases including 1036/1038/983))
- [ ] G-ARCHON-TEST-RELEASE-SKILL-053: Gate blocks but with wrong remediation text → `formatLeakError` context detection is broken (Section: Test 4 — Env-leak gate refuses a leaky .env (optional, for releases including 1036/1038/983))
- [ ] G-ARCHON-TEST-RELEASE-SKILL-054: Pass criteria: (Section: Test 5 — Isolation list works (sanity check))
- [ ] G-ARCHON-TEST-RELEASE-SKILL-055: Exit code 0 (Section: Test 5 — Isolation list works (sanity check))
- [ ] G-ARCHON-TEST-RELEASE-SKILL-056: No errors (the list may be empty if no worktrees have been created, which is fine) (Section: Test 5 — Isolation list works (sanity check))
- [ ] G-ARCHON-TEST-RELEASE-SKILL-057: Always run uninstall, even if Phase 4 failed. The goal is to leave the system in the same state as before the test. (Section: Phase 5 — Uninstall)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-058: Restore `archonstable` if it existed before the test (dualhomebrew pattern — see Phase 2 item 4): (Section: should match the dev version captured in Phase 2)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-059: Header: release version tested, install path, timestamp, SHA256 of the tested binary (Section: Phase 6 — Report)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-060: Environment: dev binary path + version (proof the dev install was not disturbed) (Section: Phase 6 — Report)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-061: Test results table: one row per test with PASS / FAIL / SKIP (Section: Phase 6 — Report)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-062: Captured output: for any FAIL, include the exact command, exit code, and last 20 lines of stderr/stdout (Section: Phase 6 — Report)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-063: Overall verdict: PASS if all tests passed, FAIL if any test failed (Section: Phase 6 — Report)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-064: Next steps: if FAIL, suggest concrete actions (file a hotfix issue, retag, check the build workflow, etc.) (Section: Phase 6 — Report)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-065: Update release notes on GitHub if not done already (Section: Phase 6 — Report)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-066: Announce on whatever channels you use (Section: Phase 6 — Report)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-067: Never touch the dev `bun link` binary. Always use the installed binary path for Phase 4 tests. Verify before and after. (Section: Key behaviors)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-068: Clean up on failure. If Phase 4 fails midway, still run Phase 5 so the next run starts clean. (Section: Key behaviors)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-069: Capture SHA256 immediately after install. This lets bug reports reference the exact artifact under test. (Section: Key behaviors)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-070: Explicit confirmation before install. Never surprise the user by installing a second binary. (Section: Key behaviors)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-071: Report the dev binary state in both preamble and postamble. Proof that the test did not disturb the dev environment. (Section: Key behaviors)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-072: Exit nonzero if any test failed. The skill should propagate failure so automated wrappers (CI, scripts) can detect it. (Section: Key behaviors)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-073: `scripts/buildbinaries.sh` — builds the binary artifacts that end up in releases (Section: Related)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-074: `.github/workflows/release.yml` — builds and publishes the binary on tag push (Section: Related)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-075: `homebrew/archon.rb` — Homebrew tap formula (updated per release) (Section: Related)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-076: `scripts/install.sh` — the curl install script (Section: Related)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-077: `scripts/installlocal.sh` / `installlocal.ps1` — localfile install harnesses (for prerelease QA of binaries built from a branch, not from GitHub releases) (Section: Related)
- [ ] G-ARCHON-TEST-RELEASE-SKILL-078: `/release` skill — the release procedure itself (opposite side of the flow) (Section: Related)
