# Active ARCHON-RELEASE Checklist: SKILL.md

- [ ] G-ARCHON-RELEASE-SKILL-001: "/release"  create a patch release (default) (Section: General)
- [ ] G-ARCHON-RELEASE-SKILL-002: "/release minor"  create a minor release (Section: General)
- [ ] G-ARCHON-RELEASE-SKILL-003: "/release major"  create a major release (Section: General)
- [ ] G-ARCHON-RELEASE-SKILL-004: "make a release", "cut a release", "ship it", "release to main" (Section: General)
- [ ] G-ARCHON-RELEASE-SKILL-005: compile \ (Section: Match the real release flags so any bundler quirk reproduces locally.)
- [ ] G-ARCHON-RELEASE-SKILL-006: minify \ (Section: Match the real release flags so any bundler quirk reproduces locally.)
- [ ] G-ARCHON-RELEASE-SKILL-007: target=bun \ (Section: Match the real release flags so any bundler quirk reproduces locally.)
- [ ] G-ARCHON-RELEASE-SKILL-008: outfile="$TMP_BINARY" \ (Section: Match the real release flags so any bundler quirk reproduces locally.)
- [ ] G-ARCHON-RELEASE-SKILL-009: Common failure modes this catches: (Section: (some module-init errors are caught by top-level try/catch but still log).)
- [ ] G-ARCHON-RELEASE-SKILL-010: Bun `bytecode` flag producing broken bytecode for the current module graph (Section: (some module-init errors are caught by top-level try/catch but still log).)
- [ ] G-ARCHON-RELEASE-SKILL-011: A dependency (e.g. an SDK) reading `package.json` or other files at module top level via paths that resolve fine in `node_modules/` but not next to a compiled binary (Section: (some module-init errors are caught by top-level try/catch but still log).)
- [ ] G-ARCHON-RELEASE-SKILL-012: Circular imports that break under minification but work under plain `bun run` (Section: (some module-init errors are caught by top-level try/catch but still log).)
- [ ] G-ARCHON-RELEASE-SKILL-013: A newly added package that ships CJS with an unusual wrapper shape (Section: (some module-init errors are caught by top-level try/catch but still log).)
- [ ] G-ARCHON-RELEASE-SKILL-014: Bump rules based on argument: (Section: Step 3: Determine Version Bump)
- [ ] G-ARCHON-RELEASE-SKILL-015: No argument or `patch` (default): `0.1.0 > 0.1.1` (Section: Step 3: Determine Version Bump)
- [ ] G-ARCHON-RELEASE-SKILL-016: `minor`: `0.1.3 > 0.2.0` (Section: Step 3: Determine Version Bump)
- [ ] G-ARCHON-RELEASE-SKILL-017: `major`: `0.3.5 > 1.0.0` (Section: Step 3: Determine Version Bump)
- [ ] G-ARCHON-RELEASE-SKILL-018: Categorize into Keep a Changelog sections: (Section: Step 5: Draft Changelog Entries)
- [ ] G-ARCHON-RELEASE-SKILL-019: Added — new features, new files, new capabilities (Section: Step 5: Draft Changelog Entries)
- [ ] G-ARCHON-RELEASE-SKILL-020: Changed — modifications to existing behavior (Section: Step 5: Draft Changelog Entries)
- [ ] G-ARCHON-RELEASE-SKILL-021: Fixed — bug fixes (Section: Step 5: Draft Changelog Entries)
- [ ] G-ARCHON-RELEASE-SKILL-022: Removed — deleted features or code (Section: Step 5: Draft Changelog Entries)
- [ ] G-ARCHON-RELEASE-SKILL-023: Writing rules: (Section: Step 5: Draft Changelog Entries)
- [ ] G-ARCHON-RELEASE-SKILL-024: Write entries as a human would — clear, concise, userfacing language (Section: Step 5: Draft Changelog Entries)
- [ ] G-ARCHON-RELEASE-SKILL-025: Do NOT just copy commit messages verbatim — rewrite them into proper changelog entries (Section: Step 5: Draft Changelog Entries)
- [ ] G-ARCHON-RELEASE-SKILL-026: Group related commits into single entries where it makes sense (Section: Step 5: Draft Changelog Entries)
- [ ] G-ARCHON-RELEASE-SKILL-027: Include PR numbers in parentheses when available: `(#12)` (Section: Step 5: Draft Changelog Entries)
- [ ] G-ARCHON-RELEASE-SKILL-028: Each entry should start with a noun or gerund describing WHAT changed (Section: Step 5: Draft Changelog Entries)
- [ ] G-ARCHON-RELEASE-SKILL-029: Skip internalonly changes (CI tweaks, typo fixes) unless they affect behavior (Section: Step 5: Draft Changelog Entries)
- [ ] G-ARCHON-RELEASE-SKILL-030: One blank line between sections (Section: Step 5: Draft Changelog Entries)
- [ ] G-ARCHON-RELEASE-SKILL-031: `package.json`: update `"version": "x.y.z"` (Section: Step 6: Update Files)
- [ ] G-ARCHON-RELEASE-SKILL-032: `pyproject.toml`: update `version = "x.y.z"` (Section: Step 6: Update Files)
- [ ] G-ARCHON-RELEASE-SKILL-033: `Cargo.toml`: update `version = "x.y.z"` (Section: Step 6: Update Files)
- [ ] G-ARCHON-RELEASE-SKILL-034: If `scripts/syncversions.sh` exists, run `bash scripts/syncversions.sh` to sync all `packages//package.json` versions to match the root version. (Section: Step 6: Update Files)
- [ ] G-ARCHON-RELEASE-SKILL-035: `package.json` + `bun.lock`: run `bun install` (Section: Step 6: Update Files)
- [ ] G-ARCHON-RELEASE-SKILL-036: `package.json` + `packagelock.json`: run `npm install packagelockonly` (Section: Step 6: Update Files)
- [ ] G-ARCHON-RELEASE-SKILL-037: `pyproject.toml` + `uv.lock`: run `uv lock quiet` (Section: Step 6: Update Files)
- [ ] G-ARCHON-RELEASE-SKILL-038: `Cargo.toml`: run `cargo update workspace` (Section: Step 6: Update Files)
- [ ] G-ARCHON-RELEASE-SKILL-039: Entry one (#PR) (Section: Added)
- [ ] G-ARCHON-RELEASE-SKILL-040: Entry two (#PR) (Section: Added)
- [ ] G-ARCHON-RELEASE-SKILL-041: Entry one (#PR) (Section: Changed)
- [ ] G-ARCHON-RELEASE-SKILL-042: Entry one (#PR) (Section: Fixed)
- [ ] G-ARCHON-RELEASE-SKILL-043: title "Release x.y.z" \ (Section: Create PR: dev -> main)
- [ ] G-ARCHON-RELEASE-SKILL-044: body "$(cat <<'EOF' (Section: Create PR: dev -> main)
- [ ] G-ARCHON-RELEASE-SKILL-045: Wait for all assets to appear on the release: (Section: Step 10: Wait for Release Workflow and Update Homebrew Formula)
- [ ] G-ARCHON-RELEASE-SKILL-046: Fetch checksums.txt and extract SHA256 values: (Section: Re-check asset count + run status after rerun.)
- [ ] G-ARCHON-RELEASE-SKILL-047: Update `homebrew/archon.rb` in the main repo atomically with version AND SHAs: (Section: Sanity check — all four must be present and non-empty)
- [ ] G-ARCHON-RELEASE-SKILL-048: Commit the formula update to main, then sync back to dev: (Section: Basic version check - archon version should exit with 0 on success)
- [ ] G-ARCHON-RELEASE-SKILL-049: Install via `brew tap coleam00/archon && brew install coleam00/archon/archon` (Section: Step 12: Verify the Release End-to-End)
- [ ] G-ARCHON-RELEASE-SKILL-050: Verify the binary reports the correct version and `Build: binary` (Section: Step 12: Verify the Release End-to-End)
- [ ] G-ARCHON-RELEASE-SKILL-051: Verify bundled workflows load (Section: Step 12: Verify the Release End-to-End)
- [ ] G-ARCHON-RELEASE-SKILL-052: Verify the SDK spawn path works (a minimal assist workflow) (Section: Step 12: Verify the Release End-to-End)
- [ ] G-ARCHON-RELEASE-SKILL-053: Verify the envleak gate is active (if shipped in this release) (Section: Step 12: Verify the Release End-to-End)
- [ ] G-ARCHON-RELEASE-SKILL-054: Uninstall cleanly (Section: Step 12: Verify the Release End-to-End)
- [ ] G-ARCHON-RELEASE-SKILL-055: Produce a PASS/FAIL report (Section: Step 12: Verify the Release End-to-End)
- [ ] G-ARCHON-RELEASE-SKILL-056: If `/testrelease brew` fails, the release is not ready to announce. File a hotfix issue for whatever broke, cut `x.y.z+1` with the fix, and rerun this skill. Do NOT advertise a release that fails `testrelease`. (Section: Step 12: Verify the Release End-to-End)
- [ ] G-ARCHON-RELEASE-SKILL-057: Do not rerun the release workflow a third time hoping it succeeds. If the failure was reproducible twice, it's a code bug — you need to ship code to fix it. (Section: Recovery: deterministic release CI failure)
- [ ] G-ARCHON-RELEASE-SKILL-058: Bundler/bytecode bug — Bun `bytecode` produces invalid output for the current module graph. Symptom: `TypeError: Expected CommonJS module to have a function wrapper` at binary startup. Historically caused by a new dependency's CJS/ESM shape interacting with `bytecode` — dropping the flag or lazyimporting the offending module has been the fix. (Section: Diagnose)
- [ ] G-ARCHON-RELEASE-SKILL-059: Moduleinit crash — a dependency does `readFileSync('package.json')` or similar at module top level via a path that exists in `node_modules/` but not next to a compiled binary. Symptom: every binary subcommand crashes immediately; error typically mentions a missing file adjacent to `process.execPath`. Fix by lazyimporting the dependency behind the code path that actually uses it. (Section: Diagnose)
- [ ] G-ARCHON-RELEASE-SKILL-060: Smoketest timeout on Windows — not actually a bug in the code; the Windows runner is slow. Rerun once; if it recurs, bump the test timeout. (Section: Diagnose)
- [ ] G-ARCHON-RELEASE-SKILL-061: Do not reuse `vx.y.z`. Cut `vx.y.(z+1)` (or nextminor if warranted) with the fix. Rationale: (Section: Fix and re-release as the NEXT patch)
- [ ] G-ARCHON-RELEASE-SKILL-062: Tag immutability: `vx.y.z` is already recorded in git history and release cache (Section: Fix and re-release as the NEXT patch)
- [ ] G-ARCHON-RELEASE-SKILL-063: Semver clarity: users and tooling should see a new version number when the bits change (Section: Fix and re-release as the NEXT patch)
- [ ] G-ARCHON-RELEASE-SKILL-064: Audit trail: "v0.3.7 was cut but had no shipped binaries; v0.3.8 is the first release with <fix>" is cleaner than rewriting v0.3.7 (Section: Fix and re-release as the NEXT patch)
- [ ] G-ARCHON-RELEASE-SKILL-065: First release with working compiled binaries after vx.y.z's <bug>. vx.y.z was tagged but its binary smoke test failed deterministically (see RUN_ID in CI history). The tag is preserved for history; this release (vx.y.(z+1)) is the first with shipped binaries. `install.sh` and Homebrew were never updated to vx.y.z, so users were not exposed to the broken state. (Section: Fixed)
- [ ] G-ARCHON-RELEASE-SKILL-066: Do not forcepush or rewrite the tag. Once a tag exists, it's a public promise of that SHA. Deleting and recreating to a different SHA is tagspoofing and breaks any downstream that cached the original. (Section: What NOT to do)
- [ ] G-ARCHON-RELEASE-SKILL-067: Do not skip this recovery path to "just push more binaries to the broken release". The release exists with a specific commit SHA; uploading binaries built from a newer SHA creates binary/source drift that is hard to diagnose later. (Section: What NOT to do)
- [ ] G-ARCHON-RELEASE-SKILL-068: Do not update the Homebrew formula before v0.3.(z+1) is fully shipped. The formula should always point at a version with all 7 assets uploaded and `/testrelease brew` passing. (Section: What NOT to do)
- [ ] G-ARCHON-RELEASE-SKILL-069: NEVER force push (Section: Important Rules)
- [ ] G-ARCHON-RELEASE-SKILL-070: NEVER skip Step 1.5 (preflight compiledbinary smoke). If the stack is a Bun/Node project with a buildbinaries script, the `bun build compile` smoke test runs before version bump, PR, or tag. Skipping it means every bundler regression or moduleinit crash only surfaces after the tag is pushed — by which point `releases/latest` is already 404ing for every user. The ~30s cost is paid to keep the failure mode local. (Section: Important Rules)
- [ ] G-ARCHON-RELEASE-SKILL-071: If Step 1.5 fails, abort the release and fix the underlying issue on a feature branch. Do not "just skip it" and hope CI doesn't repro the problem. (Section: Important Rules)
- [ ] G-ARCHON-RELEASE-SKILL-072: NEVER skip the review step — always show the changelog before committing (Section: Important Rules)
- [ ] G-ARCHON-RELEASE-SKILL-073: NEVER include "CoAuthoredBy: Claude" or any AI attribution in the commit (Section: Important Rules)
- [ ] G-ARCHON-RELEASE-SKILL-074: NEVER add emoji to changelog entries unless the user asks (Section: Important Rules)
- [ ] G-ARCHON-RELEASE-SKILL-075: If the user says "ship it" without specifying bump type, default to patch (Section: Important Rules)
- [ ] G-ARCHON-RELEASE-SKILL-076: The commit message is just `Release x.y.z` — clean and simple (Section: Important Rules)
- [ ] G-ARCHON-RELEASE-SKILL-077: NEVER update `homebrew/archon.rb` version field without also updating the `sha256` values. They must move together atomically. The correct SHAs only exist after the release workflow finishes building binaries — see Step 10. Updating the version field alone produces a stale formula that looks valid but causes checksum mismatches on install. (Section: Important Rules)
- [ ] G-ARCHON-RELEASE-SKILL-078: NEVER skip Step 11 (tap sync). The `coleam00/Archon/homebrew/archon.rb` file is only a template; users install from `coleam00/homebrewarchon/Formula/archon.rb`. If you update one without the other, users get stale or wrong data. (Section: Important Rules)
- [ ] G-ARCHON-RELEASE-SKILL-079: NEVER announce a release that failed `/testrelease brew`. A release that installs but crashes on first invocation is worse than no release — it burns user trust. If the release verification fails, cut a hotfix before telling anyone the release exists. (Section: Important Rules)
